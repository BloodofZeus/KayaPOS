Task #6: Fix SSL for Supabase connections, TS error, + database test page

Bug fixes:
1. SSL enabled on pg.Pool in server/db.ts — detects remote hosts by checking
   DATABASE_URL for localhost/127.0.0.1. Exports isRemoteHost() helper.
2. SSL enabled on session pool in server/routes.ts — imports isRemoteHost()
   from db.ts and applies same detection logic to connect-pg-simple pool.
3. TypeScript error fixed at routes.ts:255 — req.params.id narrowed to string
   via String() cast before passing to storage.updateUser().

New feature: database connection test endpoint + login page UI
4. POST /api/setup/test-db added to server/routes.ts (no auth required).
   Rate-limited to 5 req/min/IP via testDbLimiter. Accepts { url: string },
   creates a temp pg.Pool (5s timeout, SSL for remote hosts), runs SELECT 1,
   returns { ok: true } or HTTP 422 { ok: false, error }. Uses err: unknown
   with safe narrowing. Gated behind DISABLE_DB_TEST=1 env flag for
   production hardening after initial setup.
5. Login page (client/src/pages/Login.tsx) gets a collapsible "Check database
   connection" section below the login form. Collapsed by default. Expands to
   show a URL input, Test Connection button, and success (green) / error (red)
   status feedback. Calls POST /api/setup/test-db and shows the result.
6. DEPLOYMENT.md updated with DISABLE_DB_TEST env var documentation.

Code review actions addressed:
- SSRF mitigation: endpoint gated by DISABLE_DB_TEST=1 flag for production
- Replaced err: any with err: unknown + instanceof Error narrowing
- Connection errors now return HTTP 422 instead of 200 for clearer semantics

Verification: `npx tsc --noEmit` passes, server boots clean, login page
confirmed showing the collapsed DB test panel via screenshot.
